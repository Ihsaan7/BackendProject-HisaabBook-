<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Main_Page</title>
    <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
</head>

<body class="bg-gray-800 w-screen h-screen flex justify-center items-center">
    <div class="border border-white w-[80%] h-[80%] ">
        <div class="NAVBAR h-[10%] border border-white flex items-center justify-between">
            <div class="LNAV text-white px-4">
                <p>HisaabBook</p>
            </div>
            <div class="RNAV text-white flex gap-8 px-4">
                <a href="/">
                    <p>Home</p>
                </a>
                <a href="/create">
                    <p>Create Hisaab</p>
                </a>
            </div>
        </div>
        <div class="MAIN p-4 h-full ">
            <div class="text-white text-xl pl-6 pt-6">
                <h3>All Hisaab Here</h3>
            </div>
            <div class="mt-5 flex flex-col gap-2 h-fit">
                <% listArray.forEach(items=> { %>
                    <div class="flex justify-between p-3 w-[60%] h-20 border border-white text-white ">
                        <div class="flex gap-2">
                            <h4 class="">
                                <%= items.now %>
                            </h4>
                            <a href="/hisaab/<%= items.id %>/<%= items.now %>">
                                <p>✅</p>
                            </a>
                        </div>
                        <div class="flex gap-5">
                            <a href="/edit/<%= items.id %>/<%= items.now %>">
                                <p>🛠️</p>
                            </a>
                            <form action="/del/<%= items.id %>" method="POST" style="display:inline;">
                                <button type="submit">🚫</button>
                            </form>

                        </div>
                    </div>
                    <% }) %>
            </div>
        </div>
    </div>
</body>

</html>